Monster Reolodex

    when to render or rerender the webpage parts is an ultimate goal

function return AJAX
class render the returned AJAX

define state in constructor

change state with setState for shallow merge

change state properly with nested objects for rendering

this.setState(function update, callbask function)

if we have million states then what
save in array
do it with map function with return

each child should have unique  key
add an id

puropse of key of we have to update one value, that key value pair should be changes not all 


SPA - single page application
A single-page application (SPA) is a web application or website that interacts with the user by dynamically rewriting the current web page with new data ...



////

git add -A
git commit -m "day1"
git branch -M main 
git push -u origin main